// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // No integrations needed for Sass, just install 'sass' and use <style lang="scss"> in your components.
});