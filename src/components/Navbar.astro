<nav>
    <div class="container">
        <a href="/#" class="logo">
            <img src="/logo.png" alt="logo" />
        </a>

        <!-- Hamburger Menu Button -->
        <button class="hamburger" id="hamburger" aria-label="Toggle menu">
            <span></span>
            <span></span>
            <span></span>
        </button>

        <!-- Navigation Menu -->
        <div class="menu" id="menu">
            <ul>
                <li>
                    <a href="/#">Inicio</a>
                </li>
                <li>
                    <a href="/#seguros">Servicios</a>
                </li>
                <li>
                    <a
                        class="btn btn_primary"
                        href="https://wa.me/2228340689"
                        target="_blank">Cotiza vía WhatsApp</a
                    >
                </li>
            </ul>
        </div>
    </div>
</nav>

<script>
    // Hamburger menu toggle functionality
    const hamburger = document.getElementById("hamburger");
    const menu = document.getElementById("menu");

    hamburger?.addEventListener("click", () => {
        hamburger.classList.toggle("active");
        menu?.classList.toggle("active");
    });

    // Close menu when clicking on a link (mobile)
    const menuLinks = menu?.querySelectorAll("a");
    menuLinks?.forEach((link) => {
        link.addEventListener("click", () => {
            hamburger?.classList.remove("active");
            menu?.classList.remove("active");
        });
    });
</script>

<style lang="scss">
    nav {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 4rem;
        padding: 3rem 0;
        z-index: 1000;
        backdrop-filter: blur(8px);

        .container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 100%;
        }

        .logo {
            img {
                width: 70px;
            }
        }

        // Hamburger Menu Button (only visible on mobile)
        .hamburger {
            display: none;
            flex-direction: column;
            background: none;
            border: none;
            cursor: pointer;
            padding: 0.5rem;
            z-index: 1001;

            span {
                width: 25px;
                height: 3px;
                background: #2c2c2c;
                margin: 3px 0;
                transition: all 0.3s ease;
                border-radius: 2px;
            }

            &.active {
                span:nth-child(1) {
                    transform: rotate(45deg) translate(9px, 9px);
                }

                span:nth-child(2) {
                    opacity: 0;
                }

                span:nth-child(3) {
                    transform: rotate(-45deg) translate(7px, -6px);
                }
            }
        }

        .menu {
            ul {
                display: flex;
                align-items: center;
                gap: 2rem;

                li {
                    list-style: none;

                    a {
                        color: #2c2c2c;
                        font-size: 1rem;
                        font-weight: 400;
                        text-decoration: none;

                        &:hover {
                            color: #d83636;
                            transition: all 0.1s ease-in-out;
                        }

                        // Override navbar link colors for button
                        &.btn_primary {
                            color: white !important;
                            text-decoration: none;

                            &:hover {
                                color: white !important;
                                transform: translateY(-2px);
                                box-shadow: 0 4px 12px rgba(216, 54, 54, 0.3);
                            }
                        }
                    }
                }
            }
        }

        // Mobile responsive - keep original menu hidden approach
        @media (max-width: 900px) {
            .hamburger {
                display: flex;
            }

            .menu {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100vh;
                background: rgba(246, 249, 253, 0.98);
                backdrop-filter: blur(8px);
                display: flex;
                align-items: center;
                justify-content: center;
                transform: translateX(-100%);
                transition: transform 0.3s ease;

                &.active {
                    transform: translateX(0);
                }

                ul {
                    flex-direction: column;
                    gap: 2rem;
                    text-align: center;

                    li {
                        a {
                            font-size: 1.2rem;
                            display: block;
                            padding: 0.75rem 1.5rem;

                            // Mobile CTA button styling handled by global btn btn_primary classes
                            &.btn_primary {
                                margin-top: 1rem;
                                display: inline-block;
                                text-decoration: none;
                                color: white !important;

                                &:hover {
                                    color: white !important;
                                    transform: translateY(-2px);
                                    box-shadow: 0 4px 12px
                                        rgba(216, 54, 54, 0.3);
                                }
                            }
                        }
                    }
                }
            }
        }
    }
</style>
